{
  "name": "whoami",
  "version": "1.0.0",
  "description": "Pulls certain data from http header and sends back in JSON format",
  "main": "server.js",
  "scripts": {
    "test": "jshint **.js",
    "serve": "npm test -s && npm start"
  },
  "author": "Jon Lucas",
  "license": "MIT",
  "devDependencies": {
    "jshint": "^2.9.1"
  },
  "dependencies": {
    "express": "^4.13.4"
  }
}
